---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: smartworkx/gradle-alpine

inputs:
  - name: sources

outputs:
  - name: dist

run:
  path: sources/pipeline/dist.sh
